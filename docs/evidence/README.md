# Optional visual evidence

This folder is a placeholder. It does not contain screenshots or video.

If you add real captures for assessors, keep them here (or attach them outside git) and link them from [submission-evidence.md](../submission-evidence.md).

Suggested names **only after** the files actually exist:

- `github-actions-green.png`
- `railway-api-active.png`
- `railway-web-active.png`
- `web-convert-verified.png`
- `betway-ui-generated-code.png`
- `android-decode-canonical-slip.png`

Rules:

- Do not commit cookies, tokens, `accountId`, `DATABASE_URL`, or session headers
- Crop or redact Railway internal IDs if they appear
- Prefer current booking codes over expired reconnaissance codes
- Do not add placeholder or generated images
