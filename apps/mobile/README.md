# Mobile

Flutter is added in a dedicated later commit so the Git history clearly shows when the mobile deliverable was introduced.

Planned scope:

- booking-code input
- decoded canonical betslip view
- loading / validation / upstream-error states
- Android APK build
- iOS signing/distribution note
